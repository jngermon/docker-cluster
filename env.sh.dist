export BOOTSTRAP=false
export NODE_NAME={ the node name }
export NODE_IP={ the node IP }
export ENCRYPT={ the encrypt key common to all node in cluster }
export DATACENTER_NAME={ the datacenter name }
export MANAGERS_IPS='{ list of managers nodes IP with '"' and separate by ';'}'
export CONSUL_UI_PORT={ the chosen port for consul ui, it can be 8500 (only needed for master node) }
export CONSUL_MASTER_IP= { the consul manager IP (= NODE_IP in server case) }
